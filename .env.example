# URL del backend (API de Notion). Server-side only.
BACKEND_URL=https://delacosta-back-production.up.railway.app

# Si el backend tiene API_KEY configurada, ponla aquí (server-side, no se expone
# al navegador). Déjala vacía si el backend no exige key.
BACKEND_API_KEY=
